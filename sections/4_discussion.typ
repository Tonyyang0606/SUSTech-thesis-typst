#import "../template/template.typ": *

= Discussion

讨论重点包括：
- ABI 差异的可复现性：同一程序多次运行是否稳定；
- 不兼容类型聚类：是集中在参数校验、权限检查，还是文件状态机逻辑；
- 工程可修复性：是否可通过补齐 syscall 语义或调整 errno 映射快速收敛。

同时，LLM 分析用于缩短定位时间，但最终结论仍需要内核实现与测试用例双重验证。
